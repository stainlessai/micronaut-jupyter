datasources:
  default:
    #url: jdbc:h2:file:/tmp/kernelTestDb;USER=sa;PASSWORD=
    url: jdbc:h2:mem:kernelTestDb
    driverClassName: org.h2.Driver
    username: sa
    password: ''
    schema-generate: CREATE_DROP
    dialect: H2
    pooled: true
jpa:
  default:
    properties:
      hibernate:
        hbm2ddl:
          auto: update
