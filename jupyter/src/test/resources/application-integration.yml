micronaut:
  server:
    port: 8080
    host: 0.0.0.0
  management:
    endpoints:
      health:
        enabled: true
        sensitive: false
      all:
        enabled: true
        sensitive: false

# Needs to be enabled to expose the InstallKernel bean referenced in the notebook for BeansTest
jupyter:
  kernel:
    install: true